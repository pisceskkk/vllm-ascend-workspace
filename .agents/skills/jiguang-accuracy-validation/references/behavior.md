# Accuracy evidence

Record the Jiguang Run ID and URL, account-owned resource identifiers, exact repository and submodule SHAs, model and weight identity, dataset/version/split, evaluation configuration, container generation, runtime/native hashes, timestamps, terminal status, raw result locations, and checksums.

Treat the Jiguang archive as the source of truth for platform raw logs and results. Keep the local Run Manifest independent and store only the traceable archive pointer and canonical summary digest.
