interface:
  display_name: "Jiguang Accuracy Validation"
  short_description: "Run archived accuracy evaluations on Jiguang"
  default_prompt: "Use $jiguang-accuracy-validation to run this committed branch through Jiguang accuracy evaluation."
dependencies:
  tools:
    - type: "mcp"
      value: "jiguang"
      description: "Account-owned Jiguang evaluations and archives"
policy:
  allow_implicit_invocation: true
