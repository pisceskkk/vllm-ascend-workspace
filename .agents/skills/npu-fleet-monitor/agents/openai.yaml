interface:
  display_name: "NPU Fleet Monitor"
  short_description: "Locate or deploy the standalone vaws-top service"
  default_prompt: "Use $npu-fleet-monitor to locate or deploy vaws-top, then hand off to its project-local Agent Skill."
