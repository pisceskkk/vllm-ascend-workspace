# Copy this file to an ignored local path such as:
#   .vaws-local/proxy/credentials.env
# Replace both placeholder values. Keep the real file untracked.

export VAWS_PROXY_USERNAME='replace-with-url-safe-username'
export VAWS_PROXY_PASSWORD='replace-with-url-safe-password'
